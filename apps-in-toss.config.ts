import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'benefit',

  brand: {
    primaryColor: '#3182F6'
  },

  permissions: [],
  webBundleDir: 'dist'
});
